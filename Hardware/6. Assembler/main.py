import SymbolTable as St
import Parser as P

# Grabs the file on the path and opens it
path = "C:\\Users\\jeszenoib\\Documents\\Nand2Tetris\\nand2tetris\\projects\\06\\add"
Parser = P.Parser(path)

# Creates the symbol table
table = St.SymbolTable("table.txt")

# We read the file and add labels to the SymbolTable



address = 0  # Stores the necessary addresses


    address += 1
